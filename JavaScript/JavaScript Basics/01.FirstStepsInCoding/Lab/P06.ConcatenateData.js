function solve(firstName, lastName, age, city) {
    let result = `You are ${firstName} ${lastName}, a ${age}-years old person from ${city}.`;
    console.log(result);
}

solve(`Plamen`, `Nedelchev`, 25, `Varna`);